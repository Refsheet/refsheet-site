#!/bin/bash

docker-compose up --no-start
docker-compose start postgres
docker-compose start redis
docker-compose start worker
docker-compose start nginx
docker-compose start webpacker
docker-compose up website