#import 'graphql/fragments/NotificationsFields.graphql'

query getNotifications {
    getNotifications {
        unreadCount
        notifications {
            ...NotificationsFields
        }
    }
}