query getForum($id: String!) {
    getForum(slug: $id) {
        id
        slug
        name
        description
        system_owned
        created_at

        discussions {
            id
            topic
            slug
            locked
            karma_total
            updated_at
            created_at
            preview
            last_read_at
            unread_posts_count
            last_post_at
            reply_count

            user {
                username
                name
                is_admin
                is_patron
                is_supporter
                avatar_url
            }

            character {
                name
                slug
                profile_image {
                    url {
                        thumbnail
                    }
                }
            }
        }
    }
}