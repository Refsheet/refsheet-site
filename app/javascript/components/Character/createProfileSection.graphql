mutation createProfileSection($characterId: ID!, $createAfterSectionId: ID) {
    createProfileSection(characterId: $characterId, createAfterSectionId: $createAfterSectionId) {
        id
        title
        row_order
    }
}