mutation createWidget($characterId: ID!, $sectionId: ID!, $columnId: ID!, $type: String!) {
    createWidget(characterId: $characterId, sectionId: $sectionId, columnId: $columnId, type: $type) {
        id
        type
    }
}