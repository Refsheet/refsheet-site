query getArtist($slug: String!) {
    getArtist(slug: $slug) {
        name,
        slug,
        guid,
        profile_markdown,
        commission_info_markdown,
        locked,
        user {
            name,
            username
        }
    }
}