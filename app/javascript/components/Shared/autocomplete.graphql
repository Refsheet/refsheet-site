query searchForUser($username: String) {
    searchForUser(username: $username) {
        name
        username
    }
}

query searchForCharacter($username: String, $slug: String) {
    searchForCharacter(username: $username, slug: $slug) {
        name
        username
        slug
    }
}